import React, {useEffect, useRef} from "react";

export const Modal = ({children}:any) => {

    return (
        <div className={'modal'}>
            <div className={'modal-content'}>

            </div>
        </div>
    )
};
